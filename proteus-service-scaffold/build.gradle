plugins {
    id 'java'
}

allprojects {
    apply plugin: 'idea'

    ext {
        protobufVersion = '3.6.0'
        proteusVersion = '0.7.19'
        springbomVersion = 'Cairo-SR1'
    }

    repositories {
        jcenter()
        mavenCentral()
    }
}